apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - locustfile-configmap.yaml
  # Scenario A: 혼합 트래픽 기준선 (50 users, 10min) - pool=10 상태
  - locust-test-scenario-a.yaml
  # Scenario B: Blue-Green 배포 검증 (50 users, 15min)
  # → 배포 테스트 시 활성화
  # - locust-test-scenario-b.yaml
  # Scenario C: 스파이크 테스트 (→200 users, 11min)
  # → 스파이크 테스트 시 활성화
  # - locust-test-scenario-c.yaml
